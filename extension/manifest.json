{
  "manifest_version": 3,
  "name": "Hypha Debugger — Browser Automation",
  "version": "0.8.4",
  "description": "Remote browser automation over Hypha RPC: AI agents can open/close/switch tabs, navigate, inspect & control pages (DOM, screenshots, click/type by index, React), and run JavaScript on any page — even strict-CSP ones (via the Chrome debugger).",
  "minimum_chrome_version": "116",
  "permissions": ["scripting", "offscreen", "sidePanel", "storage", "unlimitedStorage", "tabs", "activeTab", "alarms", "debugger"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js", "type": "module" },
  "action": { "default_title": "Hypha Debugger (open side panel)" },
  "side_panel": { "default_path": "sidepanel.html" },
  "web_accessible_resources": [
    { "resources": ["main-world.js"], "matches": ["<all_urls>"] }
  ]
}
